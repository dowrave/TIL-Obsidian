#Csharp 

- **데이터를 담는 객체를 이름 없는 클래스로 즉석에서 만드는 것.**
```cs
var data = new { value = index + 1, name = "Alice" };
```

컴파일러가 `value, name`이라는 읽기 전용 프로퍼티를 가진 클래스를 자동으로 만들어주고, `data`는 그 클래스의 인스턴스가 된다. "이름 없는 클래스의 인스턴스"라서 익명 타입으로 칭한다.

- 주의 : [[CSharp - 익명 함수|익명 함수]]와 구분.
	- 대상이 클래스냐 메서드냐의 차이 같긴 함. 

