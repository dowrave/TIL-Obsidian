#FLStudio 

1. `Playlist`에서 타임라인을 `Ctrl + 드래그`로 영역 지정

![[Pasted image 20251217021043.png]]
> 패턴이 끝나는 지점보다 조금 더 여유 있게 잡을 것. 
> 반향 등의 울릴 시간을 줘야 하기 때문임.

2. `File - Export`
	- `wav` 파일은 용량이 크고 정보를 거의 유지
	- `mp3` 파일은 용량이 작지만 일부 정보를 손실
	- 여기선 `wav` 파일로 진행... 하려고 했으나 여기 첨부도 할 겸 `mp3` 파일로 해봄

![[Example.mp3]]

> 와 옵시디언 이거 첨부도 되네

