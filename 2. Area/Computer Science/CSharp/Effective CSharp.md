

## 2. .NET 리소스 관리


## 3. 제네릭 활용



## 4. LINQ 활용

### 29. 컬렉션을 반환하기보다 `Iterator`을 반환하는 게 더 낫다

### 30. 루프보다 쿼리 구문이 낫다

### 31. 쿼리 구문과 메서드 구문을 적절히 혼용하라

### 32. Action, Predicate, Func과 델리게이트를 분리하라

### 33. 필요한 시점에 필요한 요소를 생성하라

### 34. 함수를 매개변수로 사용해 결합도를 낮추라

### 35. 확장 메서드는 절대로 오버로딩하지 말라

### 36. 쿼리 표현식과 메서드 호출 구문이 어떻게 대응되는지 이해하라

### 37. 쿼리를 사용할 때는 즉시 평가보다 지연 평가가 낫다

### 38. 메서드보다 람다 표현식이 낫다

### 39. 델리게이트나 람다 표현식 안에서 예외를 던지지 말라

### 40. 지연 수행과 즉시 수행을 구분하라

### 41. 값비싼 리소스를 캡처하지 말라.

### 42. `IEnumerable` 데이터 소스와 `IQueryable` 데이터 소스를 구분하라

### 43. 쿼리 결과의 의미를 명확히 하라

### 44. 바인딩된 변수는 수정하지 말라

## 5. 예외 처리

### 45. 메서드의 실패를 알리기 위해 예외를 사용하라

### 46. 리소스 정리를 위해 `using`과 `try/finally`를 이용하라

### 47. 사용자 지정 예외 클래스를 완벽히 작성하라

### 48. 강력한 예외 보증을 준수하는 것이 좋다

### 49. catch 후 예외를 다시 발생시키는 것보다, 예외 필터가 낫다

### 50. 예외 필터의 다른 활용 예를 살펴보라