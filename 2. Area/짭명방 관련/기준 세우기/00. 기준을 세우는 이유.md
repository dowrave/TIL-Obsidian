#유니티-기준세우기

- **코드의 동작 자체에는 크게 상관은 없는 부분이지만, 나중에 다시 봤을 때 '왜 이렇게 했지?'라는 의문이 드는 지점**이 종종 있었음.
	- 예를 들면 스크립트에서 쓰는 전역 변수명은 `_`을 포함한 `camelCase`인데, `[SerializeField]`는 `_`을 붙이지 않는 것. 나중에 보면 좀 의아했다.
- 그래서 이 폴더는 어떤 걸 구현했을 때 왜 그렇게 구현했는지, 아니면 일반적인 유니티 환경에서 구현하는 방식을 다룬다. 