
>[!done]
>- 프론트엔드
>	- 게시판 : 글쓰기 버튼 상단에도 추가
>	- 글쓰기 : `list` 제거
>		- 복붙할 때 불편해서 아예 빼버렸음
>	- CSS : `ql-indent` 시리즈의 회색으로 작게 글씨를 변경한 부분들 제거
>- 백엔드
>	- 페이지네이션이 정상적으로 적용되지 않고 모든 글을 가져오는 현상 수정



## 프론트엔드
- 게시판 : 글쓰기 버튼 상단에도 추가
- 글쓰기 : `list` 제거
	- 붙여넣기 할 때 포맷팅이 제대로 적용되지 않는다. 그냥 `-`으로 달아놓고 복붙해서 그대로 쓰는 게 훨씬 편할 듯. `-`이 너무 길면 나중에 바꿔도 되고.
-  CSS : 리스트에서 탭을 누르면 적용되는 `ql-indent-1, ql-indent-2`의 회색 글씨로 작게 표기되는 부분들 모두 제거(어차피 위에서 `list` 제거하면서 앞으로 쓰는 글들은 적용되지 않음)


## 백엔드
- 페이지네이션이 정상적으로 적용되지 않고 모든 글을 가져오는 현상 수정
```python
serializer = PostListSerializer(paginated_posts, many = True, context = {'request' : request})
```
이 코드가 중복으로 있었다. 한 줄 제거.