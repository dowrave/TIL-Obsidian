#FLStudio 

## Arranging
- 아이디어나 비트를 곡에 녹여내는 일
- 곡에 새로운 요소를 추가하거나 바꾸거나 등등

- `intro - verse - chorus - verse - chorus - breakdown(bridge) - another chorus or double chorus - Outro` 같은 식으로 구성한다고 함
	- 물론 구성은 본인 마음임 : `intro`를 뺀다든가.
	- `verse`와 `chorus`의 악기 구성을 다르게 해서 `chorus`를 강조할 수 있음
	- 코러스는 훅이나 메인 멜로디가 놓이는 곳
	- 여기선 **모두 (보컬 없이) 악기로 구성하고, 코러스는 리드 멜로디 파트**
	- `riser`나 `reverse`로 코러스 전의 빌드업을 구성할 수 있음
	- 코러스 진입 직전의 `crash`도 방법

... 말로 풀면 장황해지지만 사실 다 어디선가 들어봤을 거임

- 각 파트의 길이
	- `verse` : 12 ~ 16마디
	- `chorus` : 8 마디
	- `breakdown` : 원하는 만큼 - 빌드업 길이에 의존
	- `bridge` : 8마디
		- 일렉트로닉에서 `bridge`나 `breakdown`은 8마디 이상이 되기도 함 

## 구조화
- 지금까지 했던 악기, 트랙의 이름 + 색 + 믹서 연결 등등을 관리
- 여기선 `Playlist`의 마디에 어떤 파트인지 표시하는 것도 있음

- **`Playlist`에서 마디 표시하는 부분을 클릭하고, `alt + T`를 누르면 마커를 달아놓을 수 있다.**
![[Pasted image 20260118215800.png]]
- 여기선 `intro(4) - verse(8) - chorus(8) - verse2(8) - chorus2(8) - bridge(24) - chorus3(8) - outro(8)`로 구성

- `Playlist`을 구성할 때, 리듬을 구성하는 `Drum`이 맨 위로, 그 다음이 베이스 순으로 오는 게 좋다. **리듬 파트는 맨 위에 있는 게 좋음.**
- 플레이리스트의 각 트랙을 구성할 때 `Auto name Clips`라는 게 있다. 해당 `Track`의 이름과 그 안에 들어간 패턴들의 이름을 바꾸는 것으로 보임
- 트랙들의 색과 이름을 바꿔줬음
- 트랙은 아래처럼 구성됨.
	- `Drums`
	- `Bass`
	- `Keys`
	- `Lead`
	- `FX`

### 코드와 리드 파트 분리
- 기존 `Keys`에 있던 멜로디를 진행하는 부분을 지정한 후 `ctrl + x`
- `Lead`라는 패턴을 만들고, `FLEX - Arksun Cityscape - Pure Feather`로 악기 지정
- 피아노 롤을 켜서 붙여넣기
### 구조화 계속

- `Channel Rack`에 나타난 악기와 이름을 `PlayList`에서의 구성처럼 옮겨준다. 
![[Pasted image 20260118221744.png]]
- `Keys`의 악기는 원래 `FL Keys`였으나 `3x Osc`로 바꿨음

>[!note]
>- **`Channel Rack`에서의 악기 순서 변경은 `alt + 화살표`** 임
>	- **우클릭했을 때 이동시키는 메뉴가 없다. 알아두자.**

## 더 추가하기

### Arpeggiator

- 아르페지오는 일렉트로닉 댄스, 혹은 일렉트로닉 장르 자체에서도 많이 쓰임

- 기존 `Lead` 패턴을 `Chorus`로 밀고 
- `Arpeggio`라는 패턴, 악기`Flex - Pure Feather`, 트랙, 믹서 등등에 이름 & 색상 설정해서 추가

- 기존 코드 진행 : Am - G - F - G

- 이번에는 **`기준음 + 5번째 위 + 한 옥타브 위의 음`을 사용**해볼 것
	- 메이저인지 마이너인지 판단할 수 없음
	- `A2 E3 A3` - `G2 D3 G3` - `F2 C3 F3` - `G2 D3 G3`
	- 예시 : `A2(1)` `B2(2)` `C3(3)` `D3(4)` `E3(5)` `F3(6)` `G3(7)` `A3(8)`

![[Pasted image 20260119230411.png]]

- 이 상태에서 노트들을 다 잡고`Ctrl + A` 
- **`Alt + A`로 `Arpeggiator`를 켤 수 있음**

![[Pasted image 20260119230501.png]]
- `Pattern` : `Default`로 지정
- `Time Mul` : 왼쪽으로 돌리면 노트를 더 잘게 쪼갬, 오른쪽으로 돌리면 덜 쪼갬
- `Gate` :  `Time Mul`로 쪼개진 각 노트의 길이를 더 수정함. `Time Mul`을 설정한 상태에서의 노트 자체의 길이 설정.

- 강의에선 아르페지에이터를 끄고 직접 노트를 찍었다.
![[Pasted image 20260119231212.png]]

- 플레이 리스트에선 이렇게 찍으며(Drum을 `Verse`의 뒤로 밀었음)
![[Pasted image 20260119231423.png]]

- 추가로 `Arpeggio`의 소리 크기도 줄여줌 : 각 노트를 (0.5 0.25 0.2) 정도로 만져줌
### 현재 Arpeggio는 4개의 코드로 구성됨

> 참고 : `Playlist`는 `Bars`로 스냅을 설정하면 쉽다

- 4개의 마디를 1개로 줄여서 1개의 코드만 나타나게 찍는다.
- 기존의 4개 마디는 `Chorus`로 밀어버리고, `Verse`까지의 나머지는 다 `Am`으로만 설정함
	- 조작을 모르겠다? : 기존 4개의 코드로 된 패턴을 1개로 줄인 뒤, 페인트 툴 상태에서 해당 패턴을 클릭으로 찍으면 1개의 패턴을 쭉 찍을 수 있는 상태가 됨
- 마찬가지로 `Bass, Keys`도 가장 앞의 코드만 남긴다. 

- `Intro, Verse`
![[Pasted image 20260119232117.png]]

- `Chorus`
![[Pasted image 20260119232137.png]]

- `Verse2`, `Chorus2`도 비슷하게 찍는다.

## 파트 추가

### Keys OffBeat
- 패턴 `Keys Offbeat` 및 관련된 요소들 추가 
	- Tip) 믹서도 `alt + 화살표(좌우)`로 그 위치를 바꿀 수 있다. 숫자도 함께 바뀜
	- `Channel Rack`의 `alt + 화살표(상하)`와 함께 써서 요소들을 잘 뭉쳐놓자.

- 악기 : `FLEX - Squidgy Tissue`

- `Offbeat`의 구성과 역할
	- 코드 중 2개의 음으로만 구성 - 너무 꽉 차게 들리지 않게 하기 위해 
	- `offbeat`는 움직임을 줌

- `Am - G - F - G`이었다. `A5`에서 시작.
![[Pasted image 20260119233302.png]]
> - 마이너 코드는 3개 위, 메이저 코드는 4개 위에서 찍었으며
> - 위에서 말했듯 코드 중 2개의 음만 사용해서 꽉 채우지 않았음

- 지금 추가한 파트 **`Keys Offbeat`는 `Chorus`에 넣고 테스트**

### Shaker
- `Drum` 근처에서 구성
- 악기 : `FPC Egg 1 & 2`
	- 믹서에서 **같은 채널로 설정**함
- 설정
	- `Channel Rack`에서 `Fill Each 2 steps`
	- 다른 하나는 똑같이 진행하고 `Rotate Left`까지
	- `Velocity` 값들을 일정하지 않게 구성
- 패턴 삽입
	- 1번째 코러스의 뒤 4마디
	- 2번째 코러스 8마디

## 드럼 수정
1. `Kicks`라는 패턴 추가, `Verse 1`의 뒤 4마디에 넣음
	- `Fill each 4 steps`로 설정
![[Pasted image 20260119234821.png]]
> 이 부분 빌드업이 진짜 단순한 건데 꽤 재밌게 들린다. 

2. `Kick and Snare` 패턴 추가. 
	- `Kick`은 위와 동일
	- `Clap, Snare`는 2, 4박에만 
	- 이 부분은 `Verse 2`의 8마디 전부를 대체

## Countermelody 추가
- `Countermelody` : 리드 멜로디만큼 선명하진 않은 2번째 멜로디. 그 자체로도 멜로디로서 기능을 하고, 리드 멜로디를 보충해준다. 

- 멜로디를 짤 때는 피아노`FL Keys`부터 시작한다. 
	- 코드가 선명하게 들리고, 신스로 바꾸는 것도 쉽기 때문임.

- `Keys`에 있는 코드 진행을 복붙해서 가져온다.
	- `Keys` 패턴 클릭 - Piano Roll에 패턴이 뜸 - `Ctrl + A` - `Ctrl + C` - `Countermelody`로 패턴 변경 - `Ctrl + V`으로 붙여넣기
	- **조심** : 피아노 롤에서 원래 악기로 붙여넣기 될 수 있음. `FL Keys` 체크할 것.

- 정상적으로 다 가져왔다면 이런 상태 : `Lead + Keys`
![[Pasted image 20260119235743.png]]

- 가져온 코드들은 **한 옥타브 낮춰줌.**

- 노트들 분석
- 앞 4마디는 무브먼트가 많고, 뒤 4마디는 롱노트가 많음

- **카운터멜로디는 그 반대로 구성해주면 됨** 
	- 앞 4마디는 롱노트가 많게, 뒤 4마디는 무브먼트가 많게. 
- 그래서 카운터멜로디는
	- 메인 멜로디를 잘 관찰해야 함
	- 리드 악기 or 보컬 뒤에서 연주하는 악기들을 생각해보면 됨 
	- "대조적", 혹은 "정반대"의 구성도 ㅇㅋ
	- `call and response, question and answer`로 생각해도 됨.

- 대조를 주는 뚜렷한 방법은 **두 멜로디의 옥타브를 다르게** 해주는 것임
- 이외에도 바로 위 예시처럼 **무브먼트가 많다면 롱노트를, 혹은 그 반대로.**

> 위가 카운터 멜로디, 아래가 메인 멜로디
![[Pasted image 20260120001154.png]]

> 코드
![[Pasted image 20260120001210.png]]

1. 4마디 속에서도 메인 멜로디의 움직임이 많다면 카운터 멜로디는 긴 노트를 잡아줌. 반대도 마찬가지고.
2. 3마디의 `B4`는 코드의 `C`에서 반음 어긋나므로 긴장감이 생기는 상황. 카운터 멜로디에서도 `E`와 `F`가 반음 차이라서 긴장감이 생긴다.
	- 근데 강사는 이 텐션이 마음에 들어서 그냥 넘어감 

대신 텐션이 생겼으니까 뒤에선 풀어줘야 함.

뒤 4마디도 똑같은 구성인데, 3번째 마디의 1번째 `D6`만 `E6`으로 올려줬다. 
- 메인 멜로디의 `B4`가 없기 때문인데.. 솔직히 이 부분은 정확하게 캐치하지 못함

> 저 'Movement' 부분만 정확하게 파악하고 넘겨도 무방해보인다.


- 이제 가져왔던 코드와 메인 멜로디는 모두 뮤트 처리.
- 악기를 바꾼다. `FLEX - Chariot Horn`
	- 설정 
	- `Master Filter`의 `CutOff`을 살짝 줄여서 고음을 죽임
	- `Delay` 증가 - 리드 멜로디와의 거리감 형성
	- 리버브도 살짝 증가.
- 옥타브도 바꾼다. 
	- 가져온 멜로디와 코드를 한 옥타브 내렸으므로 원래 멜로디와 카운터 멜로디가 충돌하는 상황임
	- `Countermelody`을 한 옥타브 내려줌. 다른 노트들을 뮤트처리했으니까 전체 노트를 잡고 내려주자.

## Bridge - 새로운 코드 짜기
- `Keys` 패턴을 복사해서 `Keys Bridge`라는 이름으로 생성

- 1마디

- `Keys`의 메인 멜로디는 `E`에서 시작
- C5의 메이저를 찍어본다 : `C5 - E5 - G5`
![[Pasted image 20260204022123.png]]
메인 멜로디가 E6, D6, C6이고 코드는 C5, E5, G5임
D6이 코드에 있지 않는다는 점은 있지만 더 다채롭기 때문에 이렇게 잡고 진행

- 2마디
![[Pasted image 20260204022311.png]]
- B, C, D로 구성된 멜로디
- 여기의 코드는 D마이너로 구성 : `D5-F5-A5`
- 코드와 멜로디의 구성이 맞지 않지만 위에서도 언급했듯 다채로운 효과도 있음

> 기준음을 어떻게 구성하는가는 자기 마음이지만, C메이저(A마이너)를 쓰고 있으니까 바닥음은 반드시 스케일에 맞춰줘야 함(흰 건반)

- 3마디
- 상승 효과를 주기 위해 1번째 마디의 기준음이 C5, 2번째 마디의 기준음이 D5 이었으므로 이번엔 E5 마이너로 구성

![[Pasted image 20260204023103.png]]
> 3마디, 4마디 함께 첨부
> 3마디의 경우 B5가 반음 차이는 메인 멜로디와 함께 연주되므로 긴장감이 부여됨

- 첫 4마디는 `C - d - e - d` 라는 구성(대문자 : 메이저, 소문자 : 마이너)

- 5~8마디는 1번째 4마디를 복붙해서 씀
	- 3, 4마디에서는 메인 멜로디와의 차이로 인한 긴장감이 부여된 반면, 7, 8마디에서는 기준음에서 한 옥타브 올라간 음이 메인 멜로디에서 함께 연주되므로 상대적인 릴리즈 효과를 줌

## Bridge - 베이스
![[Pasted image 20260204023852.png]]
> 참고용 기존 베이스

### Bass Bridge
- `Bass Bridge`라는 패턴을 `Bass`를 복붙해서 만듦

> 위의 코드 부분과 똑같이 `C - d - e - d`로 된 구성이다. 왜 이렇게 찍히는지는 아래 참조.
![[Pasted image 20260204024542.png]]

> [!question]
> 근데 왜 E3 -> F3은 메이저고 F3 -> G3은 마이너인지 모르겠음. 기준음에서 4개 올라간 게 메이저, 3개 올라간 게 마이너인 건 알겠지만 그 다음이 왜 저렇게 구성되는 건지 모르겠다.

>[!answer] 
>- 제미나이에게 물어봤는데, 이 상황은 멜로디가 이동하는 길이 `1도 음 - 3도 음 - 4도 음`으로 항상 일정한 상황임
> - C 메이저 스케일은 `C - D - E(3) - F(4)`일 때 `E - F`가 반음 차이라서 바로 찍히고
> - D 마이너 스케일은 `D - E - F(3) - G(4)`일 때 `F - G`가 온음 차이라서 2개의 텀이 있는 것
> - 복습 
> 	- 메이저 : `T T S T T T S`
> 	- 마이너 : `T S T T S T T`

이렇게 완성한 패턴은 플레이리스트의 57마디에 넣어줌

### Bass Bridge Held
그리고 이걸 또 복사해서 `Bass Bridge Held`라는 이름으로 생성해준다.
- 49, 53번째 마디에 지정
- 위의 `Bass Bridge`의 각 마디의 첫음만 마디 전체를 연주하도록 구성함, 나머지는 제거

### Bass 악기 수정
- `High`를 줄여서 거칠게 들리지 않도록 함
- 악기를 열면 `FLEX - Sponged Up`이 나오는데, 오른쪽의 `Macro - Filters` 값을 높여줌

![[Pasted image 20260204030140.png]]

> `Master Filter`도 있고 `MACROS - Filter`도 있는데, `MACROS - Filter`의 경우가 조금 더  올릴수록 뿌옇게 들리는 효과가 더 강한 듯


## Keys 악기 수정
- 기존엔 `3x OSC`였음 
- `FLEX - Arksun Cityscape - Purple Lullaby`

> 와 이것만 바뀌어도 엄청 크다.. `3x OSC`는 솔직히 좀 시끄러웠는데




