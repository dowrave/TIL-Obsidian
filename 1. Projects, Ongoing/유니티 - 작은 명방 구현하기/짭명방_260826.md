
>[!done]
>1. SaveSlotPanel 구현
>2. 기타 수정 사항과 질문/답변
>	- `SaveSlotManager`의 위치 수정
>	- 메서드를 어떻게 작성해야 하는가?
>		- 토글 메서드
>		- 기능에 따른 메서드 분리

>[!wip]
>1. SaveSlotPanel 구현
## `GameSession` 복기
```cs
// 타이틀로 돌아갈 때 호출
public static void Teardown()
{
	if (Instance == null) return;
	Destroy(Instance.gameObject);
	Instance = null;
}
```
- 타이틀로 돌아가면 GameSession이 파괴되는 로직은 이미 구현되어 있다.
	- 필드 초기화보다는 **파괴가 더 안전**함. 초기화는 일일이 체크해야 하니까.
- 자식 오브젝트 중에서 파괴될 때 이벤트 해제 로직이 잘 작성됏는지만 체크하면 됨.


## SaveSlotPanel 구현
```
SaveSlotPanel
- Layout
-- SaveSlot
-- SaveSlot(1)
-- SaveSlot(2)
```
- **여러 개 중 어느 하나만을 선택하는 과정을 작성하고 싶다면, `Toggle/ToggleGroup`이 있다.** 


### Toggle
- On/Off 상태를 가진 `Button`. 
	- `bool isOn` 상태를 들고 있음
	- 클릭시 `isOn`이 반전, `OnValueChanged` 이벤트가 발생한다. 
		- `UnityEvent<bool>`임.
	- `Group` 필드에 `ToggleGroup`을 지정하면 같은 그룹 내 다른 `Toggle`이 켜질 때 자신은 자동으로 꺼진다. 

### ToggleGroup
- 여러 `Toggle`이 같은 `ToggleGroup`을 참조하면 그중 하나만 켜지도록 강제준다.
- `Allow Switch Off` 
	- 아무것도 선택되지 않은 상태 허용 여부. 
	- 여기서는 O로 설정.


### 사용법
```
SaveSlotPanel
  Layout            <- 여기에 ToggleGroup 컴포넌트 추가
    SaveSlot        <- Toggle 컴포넌트 추가, Group = Layout의 ToggleGroup
    SaveSlot (1)    
    SaveSlot (2)    
```
1. 그룹에 `ToggleGroup` 컴포넌트 추가
2. 각 `Toggle` 요소에 `Toggle` 컴포넌트 추가, `Group`을 상단에 지정한 `ToggleGroup`으로 설정

- 현재 선택된 요소를 가져오는 방법 2가지
1. `ToggleGroup.ActiveToggles()`로 현재 켜진 토글을 가져온 다음, `GetComponent`로 컴포넌트 잡기
2. `SaveSlot`들을 별도로 관리하고 있다면 순회해서 현재 켜진 토글값 하나 가져오기

### 중간 저장 
- `SaveSlot.cs`까지 작성 완료
	- 이제 인스펙터에서 `SaveSlot`의 구체적인 사안들을 그려나가면 됨
		- 현재 스쿼드의 육성 상태
		- 마지막 클리어 스테이지
		- 누적 플레이타임
	- 이후에 이들을 필드로 구현하고, 



## 기타 수정 사항과 질문/답변

### SaveSlotManager의 위치
- **`SaveSlotManager`는 `TitleScene`에만 두기**로 했다.
	- 세션 중에 다른 세이브 슬롯에 접근시키고 싶다면 `GameManagement`에 두는 게 맞음.

### 토글 메서드 
- 이렇게 작성해도 되나?
```cs
private void InitializeUI(bool isEmpty)
{
	_emptySlotText.gameObject.SetActive(isEmpty);
	_progressContainer.gameObject.SetActive(!isEmpty);
}
```

- 판단 기준 : 빈 슬롯 / 채워진 슬롯이 상호 배타적인 상태인가? 앞으로도 그럴 가능성이 높은가? - O
- 단, `bool` 파라미터의 이슈 - 무엇이 `true`이고 `false`인지 의미가 보이지 않음 -> 호출부에서 인자를 명명하는 방식으로 작업할 수 있음.
```cs
InitializeUI(isEmpty: true);
```

### 기능에 따른 메서드 분리
- 위에선 `InitializeUI()`라는 이름으로 작성했지만, 실제로는 아래 2가지 기능이 들어가게 됨.
	1. `metadata` 유무에 따라 보여주는 요소 분리
	2. `metadata`가 있을 때 내용을 채우기

- **이 둘은 분리하는 게 좋다.** 성격이 다르기 때문.
	- 보여주는 요소 - UI 레이아웃 / 구조
	- 내용 채우기 - 데이터 바인딩
- 이 둘이 바뀌는 이유는 서로 독립적이다.


