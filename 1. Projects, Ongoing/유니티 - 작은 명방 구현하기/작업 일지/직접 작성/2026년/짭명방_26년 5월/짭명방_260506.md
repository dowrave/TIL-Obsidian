>[!done]
>- `KeyBindingSection` : 스크롤 뷰 구현
>- `OptionPopup`의 크기 변경 & 레이아웃은 유지하되 세부 사항(폰트 크기, 간격, 패딩 등등)을 수정
>- `StageScene` : 통계 패널 단축키 연결
>- 하단 오퍼레이터 배치코스트 오름차순으로 정렬
>	- 바리케이드 등의 `DeployableUnitEntity`는 무조건 맨 뒤로 오게끔 함


>[!예정]
>1. `StageScene` 통계 패널 꺼내는 단축키 구현
>2. 스테이지 시작 시, 각 버튼의 하단에 현재 할당된 단축키 표시
>	- 시작 시점에 단축키 버튼에서 이미지가 하단으로 이동해서 멈춘 뒤 서서히 투명해지는 식으로 구현할 예정 
>3. `StageScene` 하단에 오퍼레이터 배치 코스트 순서대로 정렬
>4. `KeyBindingSection`에 스크롤 뷰 구현

>[!wip]
>- `StageScene` : 게임 시작 시점에, 단축키가 할당된 버튼들의 아래에 어떤 단축키인지 애니메이션으로 나타난 다음(버튼의 위치에서 가려져 있다가 하단으로 이동) 서서히 사라지는 방식으로 생각 중
>- `StageScene` 하단 : 오퍼레이터 배치코스트 순으로 정렬

## KeyBindingSection에 스크롤 뷰 구현

### Scroll View 복습
- (이전에도 다룬 적 있는 것 같지만) `Scroll View`의 구조는 아래와 같다.
```
Scroll Rect
- Viewport (Mask, Image) // 화면에 보여질 영역
  - Content // 전체 내용물
- Scrollbar
	- Sliding Area
	  - Handle
```
- 유니티에서 직접 Scroll View를 만들 때의 구조.
1. `Scrollbar`를 쓰는 경우, `Scroll Rect`의 자식으로 `Viewport`와 `Scrollbar`가 함께 오도록 하는 구조가 권장된다.(일부 `Visibility`에서 해당 설정이 강제됨)
2. `Viewport`의 경우 `Mask` 컴포넌트가 필요하며, `Mask` 컴포넌트는 `Image` 컴포넌트를 필요로 한다. 

- 이외에도 스크롤 바에 대해서 더 정리해둘 필요가 있겠음
	- 뭐 어찌저찌 잘 됐다. 이번엔 `Scrollbar` 부분이 배경이고 `Handle`이 스크롤 영역의 크기인데, 이 경우는 `Scrollbar`가 투명한 게 더 어울렸음.

### 기타 수정 사항
- `Scroll Rect`의 구조로 고쳤기 때문에 [[짭명방_260504]]에 했던 항목 생성 자동화가 생성되는 위치도 `Content`로 수정해야 함
```cs
Transform contentTransform = GetComponent<ScrollRect>().content.transform;
```

- 초기화 버튼도 구현. 이건 프리팹에 구현은 해두되, 가장 마지막에 와야 해서 스크립트도 추가로 들어감.
```cs
// Awake에서 설정
resetButtonContainer.transform.SetAsLastSibling(); // 가장 마지막에 배치되도록 수정
resetButton.onClick.AddListener(OnResetButtonClicked);
```

#### Horizontal Layout Group 관련 이상한 현상
- 자동화된 각 항목에 들어가는 요소 레이아웃에 살짝 문제가 생겨서 `Horizontal Layout Group` 기반으로 수정하는 과정에서..

```
Horizontal Layout Group
- TMP
- Empty(간격용)
- Button
```

여기서 `Horizontal Layout Group`을 켜면 아래처럼 정체 불명의 흰색 이미지가 나타난다.
![[Pasted image 20260506164508.png]]

- 발생 원인 : `Horizontal Layout Group`에 `Image`가 있다면, 하위 레이아웃을 조절하는 과정에서 해당 이미지가 나타날 수 있다.
	- 레이아웃을 껐을 때 나타나지 않았던 이유도 궁금하긴 한데 지금은 그거까지 들어갈 건 아닌 것 같다. `Image`를 비활성화하는 식으로 수정.

#### 이외에도 `OptionPopup` 관련 여러 사항 수정
- 스크롤 구현한대놓고 왜 여기까지 왔는가 모르겠지만... 아무튼 그렇게 됐다. 

![[Pasted image 20260506174723.png]]
- 이전보다 팝업의 크기를 키웠다. 그래서 이에 어울리게끔 전체적인 레이아웃을 수정함



## `StageScene` 통계 패널 꺼내는 단축키 구현
- `backquote`, 즉 ~에서 쉬프트를 뗀 키로 넣었다.
- 옵션 자동화가 되어있기 때문에, 이제 `KeyAction`에 기능 추가 + `LocalizationManager`에 값 넣기 + `StageManager`에서 기능 할당하기 정도로 간단하게 구현할 수 있게 되었음

## StageScene - 하단 오퍼레이터 배치 코스트 오름차순으로 정렬
- `DeployableBox`는 있는데 이들을 관리하고 있는 게 없었나? 싶다. 이 부분은 만진지 오래 돼서 한 번 알아봐야 할 듯..
- 저녁 먹고 마저 함

- 이 부분은 AI한테 던져줬다. `DeployableManager` 자체가 만진지 워낙 오래됐고 구조도 `DeployableInfo`나 `DeployableUnitState` 등등 좀 복잡한 편인데, 잘 파악해서 반환해준 듯.
```cs
// 하단의 DeployableBoxes들을 오퍼레이터의 경우에만 오름차순으로 정렬
// 바리케이드 등의 deployableUnitEntity는 무조건 맨 뒤로 오게 했음
private void SortDeployableBoxes()
{
	var operatorBoxes = deployableUIBoxes
		.Where(kvp => kvp.Key.operatorData != null)
		.OrderBy(kvp => unitStates[kvp.Key].CurrentDeploymentCost)
		.Select(kvp => kvp.Value);

	var nonOperatorBoxes = deployableUIBoxes
		.Where(kvp => kvp.Key.operatorData == null)
		.Select(kvp => kvp.Value);

	foreach (var box in operatorBoxes.Concat(nonOperatorBoxes))
	{
		// sorted[i] 각각이 자신이 위치한 transform에서 형제 이동이 일어남
		// 낮은 순서대로 맨 뒤에 넣으면 자연스럽게 오름차순 정렬이 진행됨
		box.transform.SetAsLastSibling();
	}
}
```
> 여기서 **`transform`의 인덱스 조절 로직은 상위 오브젝트**(bottomPanel)를 파악하지 않아도 된다. 아무래도 `transform` 자체에 구조가 다 들어있나 봄

위 로직을 박스가 최초에 생성될 때, 오퍼레이터가 퇴각하거나 사망할 때 등에 실행하면 됨

> 이외에도 `DeployableBox`나 `DeployableManager`에 있던 요소들 수정함.


