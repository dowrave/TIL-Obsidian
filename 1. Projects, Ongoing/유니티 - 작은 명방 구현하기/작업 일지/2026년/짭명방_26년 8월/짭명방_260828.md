
>[!wip]
>- 세이브, 로드 시스템 구현
>	1. `SaveSlot`의 구체적인 형태 구현
>	2. `TitleManager`와 `SaveSlotManager`의 기능 일부를 `MainLayout`과 `SaveSlotLayout`으로 분리
>		- `Layout`에서 `Manager`에 요청을 보내서 정보를 읽어오는 방식
>- 일반적인 유니티의 코드 컨벤션을 정리하려다가 말았다. 



## 세이브 로드 시스템 구현
- 기록하면서 작업하지 않아서 간단하게 요약함

1. `SaveSlot`의 구체적인 형태 구현
2. `TitleManager`와 `SaveSlotManager`의 기능 일부를 `MainLayout`과 `SaveSlotLayout`으로 분리
	- `Layout`에서 `Manager`에 요청을 보내서 정보를 읽어오는 방식

