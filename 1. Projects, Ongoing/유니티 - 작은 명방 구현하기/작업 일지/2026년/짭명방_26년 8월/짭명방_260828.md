
>[!wip]
>- 세이브, 로드 시스템 구현


## 세이브 로드 시스템 구현

### 세이브 데이터 구조 정리
1. `PlayerData` - 하나의 세션을 관리하는 데이터 구조
2. `SaveSlotMetadata` - 타이틀 씬에서 볼 수 있는 `PlayerData`의 요약본.

이 둘의 저장 방식은 `PlayerData`가 저장된 다음 `SaveSlotMetadata`를 저장하는 방식임. `PlayerData`를 바탕으로 `SaveSlotMetadata`를 만들어내는 방식이다.


### 프리팹 필드 관리
