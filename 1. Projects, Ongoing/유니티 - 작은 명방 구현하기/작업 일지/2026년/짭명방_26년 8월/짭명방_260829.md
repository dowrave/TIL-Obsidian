>[!done]
>- 기타 이슈
>	- 이미지 비율 깨지는 현상 : [[유니티 - 9슬라이싱 스프라이트]]
>	- 공통 스타일 관리 방법...은 일단 SO만 만들어두고 이 데이터를 참조로 눈으로 색을 집어넣는 방식으로 구현

>[!wip]
>- 타이틀 씬 기능 완성하기
>	- 시작 / 계속하기에 따른 기능 분리
## 타이틀 씬 기능 완성하기

### 시작 / 계속하기에 따른 기능 구분하기

- 공통 
	- 슬롯의 클릭 자체는 해당 슬롯의 선택 여부만을 결정한다.
	- 곧바로 진입 여부로 이어지지 않음.
	- 별도의 확인 버튼과 삭제 버튼도 구현 ㄱ

- 시작하기
	- 빈 슬롯 : 바로 진입
	- 이미 있는 슬롯 : 삭제 여부를 묻는 패널이 나타남
- 계속하기
	- 빈 슬롯 : 새로운 데이터를 만들고 진입할지를 묻는 패널이 나타남
	- 이미 있는 슬롯 : 바로 진입

이 때 **"시작하기" 상태인지 "계속하기" 상태인지 여부**를 UI 요소인 `TitleSaveSlotLayout`에서 가지면 안되고, **상위 객체(매니저 / 컨트롤러)가 해당 정보를 갖고 있는 편이 좋다.** 

- UI 요소가 2가지 책임을 갖기 때문이다.
	1. 슬롯 자체를 갖고 클릭을 받는 책임
	2. 현재 모드를 아는 책임(상위 로직의 책임)

> 이 경우 `TitleManager`나 `SaveSlotManager`에서 해당 정보를 가지고 있으면 좋음. 

### 토글 관련
- `Toggle`이 여러 개 중 하나를 선택하는 개념이 아니라 각각의 `On/Off`가 별도로 꺼지는 현상이 있음
	- 프리팹 지우고 다시 올리는 과정에서 `Toggle Group`의 참조가 사라졌음. 다시 설정.

- 그런데 각 세이브 슬롯을 다시 클릭했을 때 `IsOn` 상태가 해제된다. 
	- [[짭명방_260826]]에 `ToggleGroup - Allow Switch Off`을 활성화했던 적이 있다. 이 옵션은 **이미 `On`인 토글을 사용자가 다시 클릭했을 때 `Off`로 꺼지는 걸 허용하는지 아닌지에 관한 옵션**임.

- [[유니티 - 토글(Toggle, ToggleGroup)]] 참고.

### 일단 중단
- 앞으로 할 일
	- `SaveSlotLayout`의 삭제, 확인 버튼 기능 연결
		- 시작하기, 이어하기에 다른 기능이 이어져야 함
	- `SaveSlot`의 플레이 타임 포맷 체크(글자가 겹치는 현상 있음)
	- `SaveSlotLayout`에 뒤로 가기 추가


## 기타 이슈

### 1:1 비율이 아닐 때 스프라이트 깨지는 문제
- [[유니티 - 9슬라이싱 스프라이트]]로 정리.
- `OperatorSlot`에서도 비슷한 이슈가 있었는데, `512 x 512`로 만든 선택된 상태를 나타내는 인디케이터를 실제로 x축 / y축으로 늘어난 UI에 대해 사용하려고 하면 해당 방향으로 이미지가 늘어난 형태로 나타난다.

![[Pasted image 20260829120353.png]]

- 원래는 상하좌우 테두리 두께가 다 동일한데, x축으로 긴 스프라이트는 이미지가 늘어지는 현상이 있음

#### 9슬라이싱 스프라이트
- 유니티에선 위 이슈를 해결하기 위해 `9-Slicing`이라는 기능을 제공함.

- 유니티 공식에서 가져온 이미지.
![[Pasted image 20260829120649.png]]
- A, C, G, I는 크기가 변경되지 않음
- B, H는 수평으로 스트레치 / 타일
- D, F는 수직으로 스트레치 / 타일
- E는 수평, 수직 모두 스트레치 / 타일

##### 설정법
1. 스프라이트 세팅 확인
	- `Mesh Type - Full Rect`으로 선택

2. `Sprite Editor`
	- 스프라이트의 테두리를 정의함. 
	- 위 이미지처럼 8개의 연두색 점을 이용해 경계를 잡아준다.
	- `Border`로 수치를 입력할 수도 있음. 


### 공통 스타일 관리 방법
- 삭제 버튼은 어떤 색, 확인 버튼은 어떤 색 같은 옵션들에 일관성을 주고 싶음
- 근데 지금까지 작업 과정은 이를 구현한 개별 요소를 찾아가서 해당 색을 얻어오는 개념이었음

>[!note]
>1. **테마에 관한 SO를 구현하고, 해당 SO를 사용하는 컴포넌트에 이를 직접 붙인다**
>2. 싱글턴 등으로 하나를 만들어두고 접근시키는 방법도 있지만, 전역 상태에 의존한다는 단점이 있음(기존 ResourceManager)

이지만

- 이 프로젝트에서는 지금까지 구현해온 요소들에 별도의 컴포넌트를 추가해야 하는 개념이 되므로.. 쓸데없이 작업 과정만 늘이게 된다. 
- 일단은 **여러 요소를 살펴보고 일일이 옮기는 과정으로 구현**함.

