
>[!done]
>- TitleScene 아트 구현
>	- 엣지 굵기 변경 가능하게 변경 : `MeshTopology.Lines`에서 리본 메쉬(`MeshTopology.Triangles`)
>		- 삼각형의 도는 방향에 대해 정리
>	- VertexPulse 수정 : 동심원 -> 일정 주기로 크게 깜빡이는 패턴
>		- 각도에 따른 페이딩 계산은 스크립트에서 셰이더 그래프로 옮김
>		- 어두운 버텍스에는 VertexPulse가 나타나지 않음
## TitleScene 아트 엣지 굵기 변경 가능하게 변경

### 기존 구현과 해결 방향
- 기존 방식
	- `MeshTopology.Lines`로 메쉬를 만들고 있음.
	- `Lines` 토폴로지는 GPU가 선분을 그릴 때 쓰는 별도의 렌더링 모드이며, 이 경우 셰이더 그래프의 `Vertex / Fragment` 단계에서는 선의 폭에 관여할 방법이 없다.
	- 즉, **지오메트리 자체가 폭이 없는 선**인 상황. 

- 해결 방향
	- **엣지를 실제 삼각형 리본으로 만드는 쪽으로 구조를 바꿔야 한다.**

### 구현 원리

#### 기존 : MeshTopology.Lines
- `MeshTopology.Lines`로 그리는 선은 면(삼각형)이 아니라 1차원 선분이다. 
- **GPU의 입장에서 삼각형은 내부를 채워넣는 래스터라이징 과정이 있지만, 선분은 면적이라는 개념이 없다.**
- 따라서 **화면에 선분을 그릴 때 GPU/드라이버가 선분 주변의 몇 픽셀까지를 선으로 칠할지를 자체적으로 정한다.** 이 과정은 셰이더가 아니라 GPU/드라이버에서 결정된다.
- 그렇기 때문에, 버텍스 셰이더는 점이 어디 있는가만 나타낼 수 있고, 점 주변에 얼마나 넓게 칠해야 하는가에 관한 정보는 알 수 없다. 폭이라는 정보가 지오메트리 안에 없기 때문에, 셰이더 그래프에서 조절할 수 있는 개념이 아니게 된다.

#### 변경 : 리본 메쉬
- 리본은 선이 아니라 **폭을 가진 얇고 긴 평면(면적이 있는 사각형)** 이다.  
- 겉보기엔 얇은 선으로 보이나, **원래 선분의 양 옆으로 살짝 벌어진 2개의 점을 만들어서 그 4개의 점을 삼각형 2개로 채운 것**이다.
- 따라서 점들이 벌어진 정도에 관한 위치 계산은 버텍스 셰이더가 다루는 값이 된다. 

![[Pasted image 20260918161219.png]]
- 리본 방식은 원래의 정점 위치에서 좌우로 벌어진 4개의 점을 만들고, 그 4개의 점으로 삼각형 2개를 만든다. (우상 - 좌하로 자름)
- 여기서 벌어지는 정도를 셰이더의 `EdgeThickness`로 조절할 수 있다는 의미. 

> 중간에 CSS 만지느라 시간이 좀 걸렸다. PNG 파일에 CSS가 적용되네? 신기하다.

### 구현
- `TitleSolid.BuildFlowLineMesh`
```cs
// 엣지마다 리본(폭이 있는 사각형 = 2개의 삼각형)을 만드는 메쉬.
// Tangent.xyz = 엣지에 수직인 방향(perp(endicular)). 셰이더 버텍스에서 Position += Tangent.xyz * UV.z * (EdgeThickness * 0.5) 형태로 오프셋해서 실제 두께를 만듦
public static Mesh BuildFlowLineMesh(
	List<Vector3> verts,
	List<(int, int)> edges,
	Color[] vertexColors,
	Vector3[] normals,
	int seed = 0)
{
	// 엣지마다 고유한 2개의 정점을 가져야 하므로, 관련 요소들도 다 2배로 구현
	// var newVerts = new List<Vector3>(edges.Count * 2);
	// var newColors = new List<Color>(edges.Count * 2);
	// var newNormals = new List<Vector3>(edges.Count * 2);
	// var newUVs = new List<Vector2>(edges.Count * 2);
	// var indices = new List<int>(edges.Count * 2);

	// 엣지마다 고유한 4개의 정점(a+, a-, b+, b-)을 가져야 하므로, 관련 요소들도 다 4배로 구현
	var newVerts = new List<Vector3>(edges.Count * 4);
	var newColors = new List<Color>(edges.Count * 4);
	var newNormals = new List<Vector3>(edges.Count * 4);
	
	var newUVs = new List<Vector3>(edges.Count * 4); // UV.x : 엣지의 위치(t = 0 ~ 1) / UV.y = 엣지 고유 랜덤 시드, UV.z = 리본 내 side(+1 / -1)
	var newTangents = new List<Vector4>(edges.Count * 4); // 리본으로 구현하면서 추가된 요소
	var indices = new List<int>(edges.Count * 6); // 2개의 삼각형을 그려야 하므로 메쉬당 6개의 인덱스가 필요(겹치더라도)

	// 랜덤 생성기. 
	// UnityEngine.Random은 전역 상태를 공유하므로 해당 함수가 몇 번 호출됐느냐에 따라 값이 달라질 수 있다.
	// System.Random은 함수 안에서만 독립적으로 동작하므로 재현 가능한 패턴을 만들 수 있다.
	var rng = new System.Random(seed);

	foreach (var (a, b) in edges)
	{
		// 엣지별 시드. 양쪽 정점이 동일해야 보간돼도 엣지 전체에서 같은 값을 유지함
		float edgeRandom = (float)rng.NextDouble();

		// 엣지 방향 및 양 끝 정점 노멀을 평균한 대표 노멀, 엣지에 수직인 방향(perp)을 구함
		Vector3 edgeDir = (verts[b] - verts[a]).normalized;
		Vector3 midNormal = (normals[a] + normals[b]).normalized;
		Vector3 perp = Vector3.Cross(edgeDir, midNormal);

		// edgeDir과 midNormal이 거의 평행한 경우 Cross가 0에 가까워지는 예외 상황 방지
		if (perp.sqrMagnitude < 1e-6f)
		{
			perp = Vector3.Cross(edgeDir, Vector3.up);
			if (perp.sqrMagnitude < 1e-6f) perp = Vector3.right;
		}

		perp.Normalize();
		Vector4 perp4 = new Vector4(perp.x, perp.y, perp.z, 0f);

		Color colorA = vertexColors != null ? vertexColors[a] : Color.white;
		Color colorB = vertexColors != null ? vertexColors[b] : Color.white;
		Vector3 normalA = normals != null ? normals[a] : Vector3.zero; 
		Vector3 normalB = normals != null ? normals[b] : Vector3.zero;

		// 설정할 때마다 newVerts에 값을 추가하므로, 이는 고유의 인덱스가 됨
		int baseIndex = newVerts.Count;

		// newVerts.Add(verts[a]);
		// newColors.Add(vertexColors != null ? vertexColors[a] : Color.white);
		// newNormals.Add(normals != null ? normals[a] : Vector3.zero);
		// newUVs.Add(new Vector2(0f, edgeRandom));

		// newVerts.Add(verts[b]);
		// newColors.Add(vertexColors != null ? vertexColors[b] : Color.white);
		// newNormals.Add(normals != null ? normals[b] : Vector3.zero);
		// newUVs.Add(new Vector2(1f, edgeRandom));

		// indices.Add(baseIndex);
		// indices.Add(baseIndex + 1);


		// a_+, a_-, b_+, b_- 순서로 4개의 정점을 생성함
		newVerts.Add(verts[a]); 
		newColors.Add(colorA); 
		newNormals.Add(normalA);
		newUVs.Add(new Vector3(0f, edgeRandom, +1f));
		newTangents.Add(perp4);

		newVerts.Add(verts[a]); 
		newColors.Add(colorA); 
		newNormals.Add(normalA);
		newUVs.Add(new Vector3(0f, edgeRandom, -1f));
		newTangents.Add(perp4);

		newVerts.Add(verts[b]); 
		newColors.Add(colorB); 
		newNormals.Add(normalB);
		newUVs.Add(new Vector3(1f, edgeRandom, +1f));
		newTangents.Add(perp4);

		newVerts.Add(verts[b]); 
		newColors.Add(colorB); 
		newNormals.Add(normalB);
		newUVs.Add(new Vector3(1f, edgeRandom, -1f));
		newTangents.Add(perp4);

		// indices.Add(baseIndex);
		// indices.Add(baseIndex + 1);

		// 쿼드를 삼각형 2개로 분할
		indices.Add(baseIndex + 0);
		indices.Add(baseIndex + 2);
		indices.Add(baseIndex + 1);

		indices.Add(baseIndex + 1);
		indices.Add(baseIndex + 2);
		indices.Add(baseIndex + 3);
	}

	// 메쉬 조립
	var mesh = new Mesh(); // 기본적으로 정점 인덱스를 16비트로 저장함
	if (newVerts.Count > 65535)
		mesh.indexFormat = UnityEngine.Rendering.IndexFormat.UInt32; // 정점이 2배로 늘어나므로 한도 체크

	// 리스트를 배열로 저장해서 메쉬에 채워넣음.
	mesh.vertices = newVerts.ToArray();
	mesh.colors = newColors.ToArray();
	mesh.normals = newNormals.ToArray();
	
	// mesh.uv = newUVs.ToArray();
	// mesh.SetIndices(indices.ToArray(), MeshTopology.Lines, 0);
	mesh.SetUVs(0, newUVs);
	mesh.tangents = newTangents.ToArray();
	mesh.SetIndices(indices.ToArray(), MeshTopology.Triangles, 0);

	return mesh;
}
```
> 주석 처리된 부분은 기존의 Line 기반, 수정된 부분은 그 아래에 작성됨.

##### 삼각형 만들 때 도는 방향에 관해
- 대각선을 어떤 방향으로 잡아주느냐에 따라 크게 2가지 방법이 있는데, 어느 쪽을 골라도 사각형 전체를 빈틈없이 채울 수 있다.
- 유니티는 **카메라가 보는 방향에서 정점을 나열한 순서가 시계 방향이라면 앞면(=보이는 면)이라는 규칙을 쓴다.** (OpenGL 계열은 반시계 = 앞면이라는 차이점이 있음)
	- 카메라가 보는 방향 = 이 케이스에서는 구 바깥쪽에서 표면을 들여다보는 방향.
	- `a+, b+, a-` : 좌상 - 좌하 - 우상, 반시계 방향
	- `a-, b+, b-` : 우상 - 좌하 - 우하, 반시계 방향

- 삼각형 관련 부분 처리를 시각적으로 보자.
```cs
indices.Add(baseIndex + 0); // a+
indices.Add(baseIndex + 1); // a-
indices.Add(baseIndex + 2); // b+

indices.Add(baseIndex + 1); // a-
indices.Add(baseIndex + 3); // b-
indices.Add(baseIndex + 2); // b+
```

시계 방향으로 알고 있는 부분인데, 실제로 나타나는 형태는 아래와 같다.

- `Render Face : Front`
![[Pasted image 20260918204718.png]]

- `Render Face : Back`
![[Pasted image 20260918204733.png]]

- `Render Face : Both`
![[Pasted image 20260918204751.png]]

의외의 형태가 나오고 있는 상황.

- 한편, 방향을 바꿔본다.
```cs
indices.Add(baseIndex + 0); // a+
indices.Add(baseIndex + 2); // b+
indices.Add(baseIndex + 1); // a-

indices.Add(baseIndex + 1); // a-
indices.Add(baseIndex + 2); // b+
indices.Add(baseIndex + 3); // b-
```

> `Both`는 그렇다 치고, `Front`랑 `Back`만 봄

- `Front`
![[Pasted image 20260918204942.png]]

- `Back`
![[Pasted image 20260918205008.png]]

- 따라서
1. **회전 방향에 따라서 면이 달라지는 게 맞음**
2. 근데 회전 방향이 내가 생각하는 것과는 좀 다른 것 같다. 단순히 내가 보는 시점이 아니라, 도형 안에서 밖을 보는 시점에 가까워 보임. 그래야 저 시계, 반시계 방향이 말이 된다. (밖에서 보는 것과 반대이므로)

>[!note]
> - 시계, 반시계 방향의 정확한 기준
> 	- **정점들을 카메라 기준으로 화면에 투영한 다음, 2D 좌표에서 정점이 나열된 순서.**
> 	- 따라서, 카메라가 안에서 밖을 볼 때와 밖에서 안을 볼 때의 회전 방향이 달라질 수 있다. **즉 카메라의 위치에 영향을 받는 요소**라는 것. 
> 	- 그래서 실질적으로 저런 걸 다 생각하면서 구현하는 것보다, 그냥 2개의 삼각형을 도는 순서만 일치시킨 다음 `Render Face`를 설정해보면서 어디가 앞이고 뒤인지는 눈으로 보는게 좋다. 
> 	- 도는 순서는 2D 평면도에서의 그것과 크게 다르지 않음.

> 어차피 `Both`로 쓸 거라서 어떻게 구현되든 크게 상관은 없는 문제였지만, 이 부분은 지식이 늘었다! 파트에 넣어도 좋을 듯.

- 신호의 크기 관련해서도 만져보고 싶긴 한데, 지금 수준으로도 만족스러운 시각적 효과는 나온다고 생각한다. 그래서 일단 패스.

## TitleScene VertexPulse 수정
- 이거 쓰면서 있던 일
1. 위에꺼 작업하는데 클로드에서 5시간 짜리 토큰을 다 썼음
2. 이 부분 작업하는데 클로드가 **3분** 정도 생각을 함
3. 우선 구현을 받은 다음, 추가적으로 수정하려고 하는데 토큰을 다 썼다고 함. 5시간 짜리 토큰을 질문 2개에 다 썼다..?

그래서 아래 작업은 ChatGPT로 넘어갔음.

1. `VertexPulse`를 퍼져나가는 동심원 -> 일정 주기로 깜빡이는 패턴으로 변경
2. 깜빡이는 패턴은 스크립트에 남겨두되, 카메라와 구 -> 버텍스로 향하는 벡터의 내적값에 따른 페이딩 계산은 셰이더로 넘김. 
	- 넘기는 김에 색깔도 HDR로 넣고 싶어서 함께 넘겼다.

여기서 `LateUpdate` 및 `UpdateFacingVisibility()` 부분은 GPU가 더 잘하는 계산이라고 함. 셰이더로 옮긴다. 

```cs
using UnityEngine;

[RequireComponent(typeof(MeshFilter), typeof(MeshRenderer))]
// 각 정점에 기본적으로 점이 있고, 잠깐씩 커졌다가 작아지는 효과 구현.
// 구의 노멀이 화면 반댓쪽을 보면 페이드아웃.
public class VertexPulse: MonoBehaviour
{
    private float _baseRadius;
    private float _pulseRadius;
    private float _pulseDuration;
    private MeshRenderer _meshRenderer;
    private MaterialPropertyBlock _mpb;
    private Camera _targetCamera;
    private Transform _solidRoot; // 구의 중심(부모 transform)

    private bool _isPulsing;
    private float _pulseTimer;

    // private static readonly int ColorID = Shader.PropertyToID("_BaseColor");
    private static readonly int SolidCenterID = Shader.PropertyToID("_SolidCenterWS");


    private void Awake()
    {
        _meshRenderer = GetComponent<MeshRenderer>();
        _mpb = new MaterialPropertyBlock();
    }

    public void Init(Mesh circleMesh, 
                    Material material, 
                    Camera cam,
                    float baseRadius, 
                    float pulseRadius, 
                    float pulseDuration)
    {
        GetComponent<MeshFilter>().mesh = circleMesh;
        _meshRenderer.material = material;

        _targetCamera = cam;
        _solidRoot = transform.parent; // 구의 중심

        _baseRadius = baseRadius;
        _pulseRadius = pulseRadius;
        _pulseDuration = pulseDuration;

        _isPulsing = false;
        transform.localScale = Vector3.one * _baseRadius;
        _meshRenderer.enabled = true; // 평소에도 항상 점이 보이므로

        _mpb.SetVector(SolidCenterID, _solidRoot.position);
        _meshRenderer.SetPropertyBlock(_mpb);
    }


    // 중간 크기 없이 즉시 확대, 일정 시간 뒤 원래 크기로 돌아오는 이산적인 펄스를 트리거함
    public void Pulse()
    {
        _isPulsing = true;
        _pulseTimer = 0f;
        transform.localScale = Vector3.one * _pulseRadius;
    }

    private void LateUpdate()
    {

        // 빌보드 구현: 회전은 항상 카메라 기준으로 덮어씀 (위치는 부모를 따라감)
        if (_targetCamera != null)
            transform.rotation = _targetCamera.transform.rotation;

        // 펄스 지속시간이 끝나면 보간 없이 즉시 원래 크기로 복귀
        if (_isPulsing)
        {
            _pulseTimer += Time.deltaTime;
            if (_pulseTimer >= _pulseDuration)
            {
                _isPulsing = false;
                transform.localScale = Vector3.one * _baseRadius;
            }
        }

        // UpdateFacingVisibility();
    }

    // 정점의 구면 노멀(중심 -> 정점 방향) 및 정점 -> 카메라 방향을 내적, 카메라 반댓쪽을 향하는 점을 페이드아웃시킴
    // private void UpdateFacingVisibility()
    // {
    //     if (_targetCamera == null || _solidRoot == null) return;

    //     Vector3 normal = (transform.position - _solidRoot.position).normalized;
    //     Vector3 toCamera = (_targetCamera.transform.position - transform.position).normalized;
    //     float facing = Vector3.Dot(normal, toCamera); // 1에 가까울수록 정면, -1에 가까울수록 카메라 반대

    //     // _facingFadeRange ~ +facingFadeRange 구간에서 서서히 페이드. 그 밖은 완전히 보이거나 숨겨짐
    //     float alpha = Mathf.Clamp01(Mathf.InverseLerp(-_facingFadeRange, _facingFadeRange, facing));
    //     alpha *= _color.a;

    //     _meshRenderer.enabled = alpha > 0.001f;
    //     if (_meshRenderer.enabled)
    //     {
    //         Color c = _color;
    //         c.a = alpha;
    //         _mpb.SetColor(ColorID, c);
    //         _meshRenderer.SetPropertyBlock(_mpb);
    //     }
    // }
}
```

- 셰이더 그래프(`SH_VertexPulse`)
![[Pasted image 20260918222849.png]]

- 결과물은 이런 느낌이다. 
![[Pasted image 20260918223318.png]]

마지막으로 어두운 버텍스에는 `VertexPulse`를 생성하지 않겠음
- 사실 신호가 가는 연출은 버텍스를 구분해두지 않아서 좀 거시기하긴 한데; 일단은 그렇게 처리.

```cs
// 색상도 인풋으로 받아서 조건으로 거름
private void SpawnVertexPulses(List<Vector3> verts, Color[] vertexColors)
{
	if (pulseMaterial == null)
	{
		Debug.LogWarning("[SolidBuilder] pulseMaterial이 없어 버텍스 펄스를 생성하지 않습니다.");
		return;
	}

	circleMesh = TitleSolid.BuildCircleMesh(circleSegments);
	cam = targetCamera != null ? targetCamera : Camera.main;

	// 펄스 타이밍을 어긋나게 하기 위해, 버텍스마다 독립된 루틴을 가짐
	foreach (int i in Enumerable.Range(0, verts.Count))
	{
		// 어두운 버텍스는 펄스를 생성하지 않음
		if (vertexColors[i].r < 0.99f) 
			continue;
		
		VertexPulse pulse = CreatePulseObject(verts[i]);
		StartCoroutine(VertexPulseRoutine(pulse));
	}
}
```

이외에도 디테일을 더 다듬으려고 하면 다듬을 부분이 있긴 한데(어두운 버텍스에 들어갈수록 신호의 색이 어두워진다든가...) 일단 타이틀 씬의 도형은 이 정도로 구현해두고 마친다.