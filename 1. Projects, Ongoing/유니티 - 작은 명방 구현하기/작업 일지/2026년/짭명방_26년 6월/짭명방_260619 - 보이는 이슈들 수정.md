
>[!done]
>1. `AlphaWaveLines` 추가 및 `BackgroundManager`와의 구조 수정
>2. 기타 이슈 수정
>	- 빠르게 해결된 이슈들
>		- 로컬라이제이션 이슈
>			- 스테이지 씬에서 스킬 이름 할당, 내용 설명에 언어 적용 안되는 문제 수정
>			- 진급 패널에서 스킬 내용 설명에 언어 적용 안되는 문제 수정
>			- 로딩 화면 언어 적용 안되는 문제 수정
>		- 스테이지를 중단했음에도 `GameState`가 `Battle`로 유지되는 현상 수정
>		- `KeybindingDisplay` 애니메이션 동작 안되는 현상 + 버튼 매핑 이상한 문제 수정
>		- 스테이지 씬 : 일시정지 중에 옵션 팝업 / 메인메뉴로 돌아가기 팝업을 띄웠다가 껐을 때 일시정지 오버레이가 없어지거나 일시정지가 풀리는 문제 수정
>	- `SkillIconBox`의 파괴 후 접근 이슈
>		  - `Initialize()`에서 이벤트 구독을 했는데, `OnDisable`없이 `Initialize`가 다시 되는 경우가 있었다. 이벤트 구독이 여러 개 쌓이는데 구독 해제는 1개만 해서 발생한 문제
>		  - 여기선 `Start / OnDestroy`로 연결해서 해결
>			  - `Initialize`에서 구독을 한다면 그 앞에 방어 코드로 구독 해제 코드를 1개 추가하는 것도 괜찮은 방법

## WaveLines의 각 파동에 알파 머티리얼 쓰는 WaveLines 추가
- `Add` 머티리얼을 쓰는 `WaveLines`의 뒤에, `Alpha` 머티리얼을 쓰는 검은색 `WaveLines`을 추가해줄 거임
- 이 때 두 파동의 형태가 일치하되 `lineThickness`만 달라야 하므로, 공통적으로 데이터를 관리할 곳이 필요함

- `BackgroundManager.cs`
	- 생성될 파동에 들어갈 값들을 정의해서 `WaveLines`에 전달

- `WaveLayerData`라는 구조체로 관리, 구조체의 위치는 `WaveLines` 내부
	- "파동을 어떻게 그릴 것인가"에 대한 데이터이기 때문.
	- [[짭명방_260619 - 보이는 이슈들 수정#지식이 늘었다]]에 정리.
		- 한 곳에서만 쓰면 중첩 클래스로 구현하고, 여러 곳에서 쓰이면 별도의 파일로 분리하면 될 듯?

- 자세한 코드는 생략. AI로 `LineThickness`을 제외한 파동을 구성하는 성분들을 매니저로 옮긴 다음, 매니저에서 파동의 파라미터를 만들어서 각 WaveLines에 전달하는 방식이다.

## 기타 이슈 수정

### 빠르게 해결된 이슈들
#### StageScene : OperatorInfoPanel
- 오퍼레이터 박스 클릭 시 `SkillNameText` 할당 풀려 있었음
- 세부사항에서 언어를 변경했음에도 한글로 출력됨 : `description` 대신 `GetDesc()` 사용, 로컬라이징이 적용되게 수정

---
#### OperatorPromotionPanel
- 스킬 내용 텍스트의 스마트 스트링이 적용되지 않는 현상 수정
```cs
// 스킬 설명
// skillDetailText.text = unlockedSkill.description;
skillDetailText.text = unlockedSkill.GetDesc();
```

---
#### 로딩 화면의 텍스트 로컬라이징
- 이 부분은 로컬라이징이 안 되어 있었음.
```
스테이지 로딩중 : {progress * 100:F0}%
```
> - 기존 텍스트의 형태가 이랬는데, 스마트 스트링은 자체 포맷팅이 가능하다.
> - 연산은 지원하지 않으니까, 스크립트 단위에선 100을 곱한 값을 progress에 넣고, 테이블에는 {progress:F0}으로 넣어두면 됨.

```cs
// LoadingScreen.cs
public void UpdateProgress(float progress)
{
	loadingSlider.value = progress;
	if (progress >= 1f)
	{
		progressText.text = LocalizationSettings.StringDatabase.GetLocalizedString(
			LocalizationManager.UI.TableName,
			LocalizationManager.UI.LoadingStatusComplete
		);
	}
	else
	{
		progressText.text = LocalizationSettings.StringDatabase.GetLocalizedString(
			LocalizationManager.UI.TableName,
			LocalizationManager.UI.LoadingStatusInProgress,
			arguments : new { progress = progress * 100 }
		);
	}
}
```
> 로딩 진행 중, 로딩 완료 에 대한 키값은 각각 `UI_LOADING_STATUS_IN_PROGRESS`, `UI_LOADING_STATUS_COMPLETE`

#### 스테이지 중단 시 스포너에서 발생하는 오류
- 결과 패널이 나타난 상태에서, 스포너에서 오류를 발생시키는 게 거슬려서 체크해봄
- `Spawn`이 동작하는데, 게임이 끝났음에도 `Battle` 상태가 된다. 

- 중단 시 `ConfirmationReturnToLobbyPanel`의 **`OnDisable`에서 `previousState`로 설정하는 로직** 때문에 그럼
	- `Cancel` 때는 해당 동작이 맞는데 `Confirm` 때는 이게 동작하면 안됨
	- 그래서 확인 / 취소 시의 동작을 구분함
```cs
private void OnConfirmButtonClicked()
{
	StageManager.Instance!.RequestExit();
	DisableThisPanel();
}

private void OnCancelButtonClicked()
{
	DisableThisPanel();
	StageManager.Instance!.SetGameState(previousState); // 버튼 클릭 전의 상태로 되돌림
}

private void DisableThisPanel()
{
	if (canvasGroup != null)
	{
		canvasGroup.alpha = 1f;
		canvasGroup.DOKill();
		canvasGroup.DOFade(0f, animationSpeed)
			.SetUpdate(true)
			.OnComplete(() =>
			{
				gameObject.SetActive(false);
			});
	}
}
```


#### KeyBindingDisplay 
1. 애니메이션 동작하지 않는 현상 
- 빈 오브젝트라고 `StageKeyBindingDisplayContainer`을 지워서 그렇다. 
	- **뭔지 모르면 함부로 지우지 말자..**
2. 일부 버튼의 매핑이 이상하게 되어 있었음. 다른 기능의 단축키를 보여줘서 수정.

#### Pause 상태에서 나가기 / 옵션 팝업 클릭 시 PauseOverlay 사라지는 현상
- 두 경우 모두 `PauseOverlay`를 가리는 로직을 제거하고, 자신의 팝업을 띄울 때 현재 게임의 진행 상태를 저장해뒀다가 취소 / 창을 닫았을 때 원래 게임 상태로 되돌림

---
### `SkillIconBox` : 비활성화 후에 `SelectedLocaleChanged`의 호출에 반응하는 현상
- `Initialize()`에서 이벤트 구독, 이벤트 해제는 `OnDisable`로 옮김.
- 그래도 해결이 안되네? 

#### 상황
1. 메인메뉴 씬에서는 **언어 전환에 맞춰서 잘 동작**함
2. **스테이지 씬에서만 해당 컴포넌트의 오류가 발생**함. **씬 전환 과정에서 `SelectedLocaleChanged`에 대한 구독 해제가 제대로 되지 않았다**는 의미임
3. 이 오류가 나타날 때가 있고 아닐 때가 있음. 오퍼레이터의 갯수?

```cs
private void OnDisable()
{
	ResetSkillIcon();
	LocalizationSettings.SelectedLocaleChanged -= OnLocaleChanged;
	// Logger.Log("OnLocaleChanged 해제됨");
}

void OnDestroy()
{
	button.onClick.RemoveAllListeners();
	LocalizationSettings.SelectedLocaleChanged -= OnLocaleChanged;
}
```
> - `OnDisable`에만 있었는데, `OnDestroy()`에도 추가해봄
> - 근데 `OnDisable`은 `OnDestroy` 이전에 호출되는 개념이기도 함;

#### 해결
- `SkillIconBox.Intialize()`에 언어 전환 이벤트를 구독하는 부분이 있음
```cs
    public void Initialize(OperatorSkill skillData, bool showDurationBox = false, bool showSkillName = false)
    {
	    // ...
	    
	    LocalizationSettings.SelectedLocaleChanged += OnLocaleChanged;
    }
```
> - `SkillIconBox`의 초기화는 **이 컴포넌트의 비활성화 없이 다시 이뤄질 수 있었다.** 예를 들어서 한 오퍼레이터를 클릭한 상태에서 다른 오퍼레이터를 클릭했을 때, 상태 초기화 없이 바로 스킬 아이콘 박스의 초기화가 다시 실행됐고, 그래서 **같은 이벤트에 구독이 여러 번 들어가게 되었던 것**

그래서 저 이벤트를 등록하는 부분을 `OnEnable`로 옮겨서 해결했다.
```cs
private void OnEnable()
{
	ResetSkillIcon();
	LocalizationSettings.SelectedLocaleChanged += OnLocaleChanged;
}
```

`OnEnable`을 쓰고 싶지 않다면, `Initialize`에서 구독 해제를 한 번 하고 등록하는 패턴도 가능.


- 이 경우는 데이터가 오는지 여부에 관계 없이 언어에 따른 데이터 전환 메서드가 실행됨을 감안하면 **`Start / OnDestroy` 쌍도 괜찮아 보임**
	- 실제 변경 여부는 **사용하는 메서드 안에서 특정 데이터의 유무에 따라 실행을 결정하는 패턴**이면 되니까.
	- 실제로 옮겼다.

## 지식이 늘었다

### 클래스에서 사용하는 구조체의 구현 위치
```cs
// 1. 클래스 밖에서 구현하기
public struct WaveLayerData { ... }
public class WaveLines : MaskableGraphic { ... }

// 2. 클래스 내에 중첩해서 구현하기
public class WaveLines : MaskableGraphic
{
    public struct WaveLayerData { ... }  // 클래스 안
}

// 3. 별도 파일로 분리하기
// WaveLayerData.cs
public struct WaveLayerData { ... }
```

1. 클래스 밖에서 구현하기
	- 같은 파일에 있지만 중첩되지 않음
	- `WaveLayerData`가 `WaveLines` 이외의 곳에서도 독립적인 의미를 가질 수 있을 때 적합
	- 그러나 같은 파일에 있다는 것 자체가 둘의 관련 있음을 암시
	- 단점 : 파일명과 파일 내용이 완전히 일치하지 않게 됨

2. 클래스 내에서 중첩 구현
	- **`WaveLayerData`는 `WaveLines` 이외의 곳에서는 의미가 없음**을 구조적으로 표현
	- 외부에서 `WaveLines.WaveLayerData`로 접근하므로 소속이 명확
	- 단점 : 외부 접근이 장황해짐. 사용하는 곳이 많아지면 다르게 구현할 필요성 O

3. 별도 파일로 분리
	- 여러 클래스가 동등하게 소비하는 경우
	- 직렬화 / 저장이 필요한 경우
	- 규모가 커서 파일 하나에 두기 부담스러운 경우

### 이벤트 구독 / 해제 패턴
- 일반적으로 **`Start`와 `OnDestroy`** 가 한 쌍, **`OnEnable`과 `OnDisable`** 이 한 쌍이다.
- 그런데 `Initialize`와 `OnDisable`의 쌍을 맞추고 싶은 경우도 있을 수 있음
	- 이 경우 `Initialize`가 다시 동작할 때, `OnDisable`을 거친 다음(혹은 상태 초기화가 되었는지 등등)에 동작이 이뤄지는지를 봐야 함
- 이것저것 고려해야 하니 복잡해지는데, 이 경우 `Initialize`에서는 한 번 구독 해제를 한 다음 다시 구독하는 패턴을 권장한다.
```cs
LanguageManager.OnLanguageChanged -= UpdateText;
LanguageManager.OnLanguageChanged += UpdateText;
```

- 참고 : 만약 **같은 이벤트에 같은 컴포넌트가 구독을 여러 번 했다면, 해제도 같은 횟수로 이뤄져야 한다.** 안 그러면 **Destroy된 컴포넌트가 이벤트 구독을 참조하고 있다는 오류**가 발생함

